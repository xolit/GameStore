<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="GoGames - Your ultimate destination for premium gaming experiences"
    />
    <meta
      name="keywords"
      content="GoGames, games, gaming, premium, experiences, download, play"
    />
    <meta name="author" content="Abdul Basit" />
    <link rel="icon" href="/public/logo.jpg" />
    <title>GoGames</title>
    <style>
      /* Animation Keyframes */
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes slideInLeft {
        from {
          opacity: 0;
          transform: translateX(-100px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes slideInRight {
        from {
          opacity: 0;
          transform: translateX(100px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes scaleUp {
        from {
          opacity: 0;
          transform: scale(0.8);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      /* Animation Classes */
      .animate {
        opacity: 0;
      }

      .fade-in {
        animation: fadeIn 1s ease-out forwards;
      }

      .slide-left {
        animation: slideInLeft 0.8s ease-out forwards;
      }

      .slide-right {
        animation: slideInRight 0.8s ease-out forwards;
      }

      .scale-up {
        animation: scaleUp 0.6s ease-out forwards;
      }

      /* Delay Classes */
      .delay-1 {
        animation-delay: 0.2s !important;
      }
      .delay-2 {
        animation-delay: 0.4s !important;
      }
      .delay-3 {
        animation-delay: 0.6s !important;
      }

      /* Section Transition */
      section {
        transition: transform 0.6s ease-out, opacity 0.6s ease-out;
      }

      /* responsive */
      /* Responsive Design */
      @media (max-width: 768px) {
        section {
          padding: 3rem 1rem;
        }

        .game-card {
          border-radius: 15px;
          margin: 1rem auto;
          width: 90%;
        }

        .download-btn {
          border-radius: 25px;
          width: 80%;
          font-size: 0.9rem;
          padding: 0.8rem;
        }

        #home {
          background: none !important;
          background-color: #fff;
        }

        #home h1 {
          font-size: 2.2rem;
          text-align: center;
        }

        #home .sec-text {
          font-size: 1.2rem;
          padding: 0 1rem;
        }

        .contributors {
          border-radius: 20px;
          margin: 1rem auto;
          width: 90%;
        }

        footer .social-links a {
          font-size: 12px;
          margin: 0 5px;
        }
      }

      /* Extra Small Devices (Phones) */
      @media (max-width: 480px) {
        .games-container {
          display: flex;
          flex-direction: column;
          gap: 1rem;
        }

        .game-card {
          width: 100%;
          padding: 1rem;
        }

        #home h1 {
          font-size: 1.8rem;
        }

        #home .sec-text {
          font-size: 1rem;
        }

        .download-btn {
          width: 90%;
          font-size: 0.85rem;
        }

        footer .social-links a {
          font-size: 10px;
        }
      }
      /* style  */
      :root {
        --bg-dark: #0a0a0a;
        --bg-medium: #1a1a1a;
        --accent: #00ff9d;
        --text: #ffffff;
        --secondary: #2a2a2a;
        --secondary-text: yellow;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Segoe UI", sans-serif;
        scroll-behavior: smooth;
      }

      body {
        background-color: var(--bg-dark);
        color: var(--text);
        line-height: 1.6;
      }

      /* Navbar Styles */
      .Navbar {
        background-color: var(--bg-medium);
        color: var(--text);
        height: 15%;
        width: 100%;
        display: flex;
        align-items: center;
        padding: 0 2rem;
        position: fixed;
        top: 0;
        left: 0;
        z-index: 10;
      }

      /* Logo */
      .logo {
        list-style: none;
        font-size: 1.5rem;
        font-weight: bold;
      }

      /* Navigation Links */
      .nav-links {
        height: 100%;
        width: 100%;
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
      }

      /* Logout Button */
      .logout-btn {
        background-color: var(--secondary);
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 5px;
        color: var(--text);
        font-size: 1rem;
      }
      .logout-btn:hover {
        background-color: red;
        animation: fade-in 0.5s ease-out;
      }

      /* Section Styles */
      section {
        min-height: 100vh;
        padding: 6rem 2rem 4rem;
      }

      /* Home Section */
      #home {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        min-height: 100vh;
        position: relative;
        overflow: hidden;
      }

      #home::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url("landScape_lgo_gogames.jpg");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        background-attachment: fixed;
        filter: brightness(30%);
        z-index: -1;
      }

      #home h1 {
        font-size: 3rem;
        margin-bottom: 1rem;
        color: var(--accent);
      }

      #home .sec-text {
        font-size: 1.5rem;
        color: var(--secondary-text);
      }

      /* Games Section */
      #games {
        background-color: var(--bg-medium);
      }

      .games-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        padding: 2rem 0;
      }

      .game-card {
        background: var(--secondary);
        padding: 2rem;
        border-radius: 15px;
        text-align: center;
        transition: transform 0.3s ease;
      }

      .game-card:hover {
        transform: translateY(-10px);
      }

      .download-btn {
        background: var(--accent);
        color: var(--bg-dark);
        padding: 0.8rem 1.5rem;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        margin-top: 1rem;
        font-weight: bold;
        transition: all 0.3s ease;
      }

      .download-btn:hover {
        background: #00cc7a;
      }

      /* About Section */
      #about {
        text-align: center;
        padding: 4rem 2rem;
      }

      /* Contributors Section */
      .contributors {
        background: var(--bg-medium);
        padding: 4rem 2rem;
        text-align: center;
      }

      /* Footer */
      footer {
        background: var(--secondary);
        padding: 2rem;
        text-align: center;
      }

      .social-links {
        margin-bottom: 1rem;
        background-color: var(--bg-medium);
        padding: 1rem;
        border-radius: 5px;
      }

      .social-links a {
        color: var(--text);
        margin: 0 1rem;
        text-decoration: none;
        transition: color 0.3s ease;
      }

      .social-links a:hover {
        color: var(--accent);
      }
    </style>

    <script>
      function logout() {
        localStorage.removeItem("token");
        localStorage.removeItem("user");
        window.location.href = "/signup";
      }

      document.addEventListener("DOMContentLoaded", () => {
        const token = localStorage.getItem("token");
        if (!token) {
          window.location.href = "/signup";
        }

        const user = JSON.parse(localStorage.getItem("user"));
        if (user) {
          document.getElementById("username").innerText = user.username;
        }
      });
    </script>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="Navbar">
      <ul class="nav-links">
        <li class="logo" id="username">Guest</li>
        <button class="logout-btn" onclick="logout()">Logout</button>
      </ul>
    </nav>

    <!-- Home Section -->
    <section id="home">
      <h1 class="animate fade-in delay-1">Welcome to GoGames</h1>
      <p class="animate fade-in delay-2 sec-text">
        Your ultimate destination for premium gaming experiences
      </p>
    </section>

    <!-- Games Section -->
    <section id="games">
      <h2>Featured Games</h2>
      <div class="games-container">
        <% if (data.length > 0) { %> <% data.forEach((game) => { %>
        <div class="game-card">
          <h2><%= game.title %></h2>
          <p><%= game.description %></p>
          <p>Engine: <%= game.engine %></p>
          <p>Platform: <%= game.platform %></p>
          <a href="<%= game.downloadLink %>" target="_blank">
            <button class="download-btn">Download</button>
          </a>
        </div>
        <% }) %> <% } else { %>
        <p>No games available.</p>
        <% } %>
      </div>
    </section>

    <!-- About Section -->
    <section id="about">
      <h2 style="animation: slideInRight 0.8s ease-out forwards">About Us</h2>
      <p style="animation: fadeIn 1s ease-out 0.3s forwards; opacity: 0">
        Passionate Indie Developer creating immersive gaming experiences since
        2020. We specialize in developing mid-quality games with engaging
        gameplay.
      </p>
      <br />
      <p style="animation: fadeIn 1s ease-out 0.3s forwards; opacity: 0">
        Hello, I'm Abdul Basit, a web and game developer skilled in HTML, CSS,
        JavaScript, C#. With experience across diverse projects, I specialize in
        crafting innovative solutions while prioritizing efficiency, continuous
        learning, and holistic growth.
      </p>
    </section>

    <!-- Contributors Section -->
    <div class="contributors">
      <h2 class="animate slide-left">Roles</h2>
      <p class="animate fade-in delay-1">Developer: Abdul Basit</p>
      <p class="animate fade-in delay-2">UI/UX Designer: Abdul Basit</p>
      <p class="animate fade-in delay-3">Music: Abdul Basit</p>
    </div>

    <!-- Footer -->
    <footer>
      <div class="social-links">
        <a
          href="https://youtube.com/@dev_looper?si=Et6rr-abkUcYyUR7"
          target="_blank"
          >YouTube</a
        >
        <a
          href="https://www.facebook.com/profile.php?id=100072954586608&ref=ig_profile_ac"
          target="_blank"
          >Facebook</a
        >
        <a href="https://www.instagram.com/xolit_edits/" target="_blank"
          >Instagram</a
        >
      </div>
      <p>© 2025 GoGames. All rights reserved.</p>
    </footer>
  </body>
</html>
